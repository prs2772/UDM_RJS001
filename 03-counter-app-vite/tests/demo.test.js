describe('First Test', () => {
  test('Trial', () => {
    //1. Init
    //2. Stimulus
    //3. Watch behaviour
    if(false){
      throw new Error('Error');
    }
  
    const messageOriginal = 'Hello World!';
    const messageTrim = messageOriginal.trim();
    expect(messageTrim).toBe(messageOriginal);
  });
});